class GoogleWorkspaceWatcher:
    """
    STUB: For future integration (Google OAuth2, MCPs, n8n).
    This watcher will run continuously on the scheduler to check Google Workspace 
    (Calendar/Mail). If high-priority messages (e.g., from a founder) are found, 
    it returns a formatted alert string.
    """
    async def check(self, db_manager, config) -> str:
        # Placeholder for future API/MCP logic
        return None
